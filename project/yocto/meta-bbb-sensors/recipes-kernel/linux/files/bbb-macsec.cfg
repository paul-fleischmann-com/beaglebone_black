# Kernel config fragment: aktiviert das Linux-MACsec-Subsystem (IEEE 802.1AE),
# Voraussetzung für mkad/MKAdaemon (SOFTWARE-MACsec-Modus, siehe Issue #260).
# Symbolname verifiziert gegen drivers/net/Kconfig (torvalds/linux).
CONFIG_MACSEC=m
